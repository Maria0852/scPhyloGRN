# Minimal training demo

Open `scPhyloGRN_training_demo.ipynb` from the repository root. The notebook
creates deterministic synthetic inputs under `work/`, trains the actual model
for two epochs, and displays the highest-scoring candidate edges.

The example checks installation and command-line integration. It is not a
biological benchmark, and its synthetic performance metrics must not be used
as evidence for biological accuracy.
